PyQt6>=6.4.0
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
qtawesome>=1.3.0

# Deliberately NOT listed here: torch / demucs (Stem Splitter feature,
# Samples tab). They're multi-GB and must never end up in the frozen .exe
# bundle - the app installs them on demand, at runtime, into a per-user
# AppData folder instead of the environment this file describes. See
# audio/stem_deps.py (the installer) and main.spec (the build-time
# excludes that guarantee they're never bundled even if a dev happens to
# have them installed in their build venv).
